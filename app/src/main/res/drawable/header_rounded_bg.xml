<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <item
        android:id="@+id/header_rounded_bg_bottom"
        android:gravity="bottom"
        android:left="-24dp"
        android:right="-24dp">
        <shape android:shape="oval">
            <solid android:color="@color/colorDeviceDisabled" />
            <size android:height="120dp" />
        </shape>
    </item>

    <item android:id="@+id/header_rounded_bg_gradient"
        android:bottom="60dp"
        android:gravity="bottom">
        <shape android:shape="rectangle">
            <gradient
                android:angle="90"
                android:endColor="@color/colorDeviceDisabled"
                android:startColor="@color/colorDeviceDisabled" />
        </shape>
    </item>

</layer-list>